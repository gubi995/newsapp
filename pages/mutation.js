import NewsForm from '../components/NewsForm';

const Mutation = () => {
  return (
    <div id="container">
      <h2>Publish a news</h2>
      <NewsForm />
    </div>
  );
};

export default Mutation;
